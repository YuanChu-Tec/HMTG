/**
 * Telegram API 凭证配置
 *
 * 请到 https://my.telegram.org 登录后：
 * 1. 打开 "API development tools"
 * 2. 创建应用（或使用已有应用）
 * 3. 将下方的 api_id 和 api_hash 替换为你的值
 *
 * 注意：不要将真实 api_hash 提交到公开仓库，可将本文件加入 .gitignore 或使用环境变量。
 * 使用时删去.example后缀，并在下方填入真实值。
 */

/** Telegram API ID（数字，从 my.telegram.org 获取） */
export const TELEGRAM_API_ID: number = 0;

/** Telegram API Hash（字符串，从 my.telegram.org 获取） */
export const TELEGRAM_API_HASH: string = '';
